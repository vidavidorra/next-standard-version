name: Release test
on:
  push:
    branches:
      - main
jobs:
  get-version:
    if: |
      github.event.commits[0].message ==
          'chore(release): trigger release process [ci release]'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@28c7f3d2b5162b5ddd3dfd9a45aa55eaf396478b # tag=v2.3.1
        with:
          fetch-depth: 0
      - name: Fetch tags
        run: git fetch --tags
      - name: Setup node
        uses: actions/setup-node@1f8c6b94b26d0feae1e387ca63ccbdc44d27b561 # tag=v2.5.1
        with:
          node-version: '12.x'
      - name: Install project
        run: npm ci --ignore-scripts
      - name: Build (artefacts)
        run: |
          npm run build
          npm link
      - name: Get release version
        run: next-standard-version
